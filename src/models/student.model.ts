
export interface Student {
  id: string;
  name: string;
  enrollmentNumber: string;
  course: string;
  year: number;
  email: string;
  phone: string;
  photoUrl: string;
  dateOfBirth: string; // YYYY-MM-DD
}
